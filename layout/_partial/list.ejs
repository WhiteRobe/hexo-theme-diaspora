<%
  let sortedList = page.posts.sort("date", "desc").filter(function(post){
    return post.title;
  });
  sortedList.each(function(post, i){
    if (page.prev == 0 && i < start) {
        return true;
    }
%>
<%- partial('post/item', {post: post}) %>
<% 
  }) 
%>